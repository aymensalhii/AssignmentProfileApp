export class Company {
    name: string;
    bs: string;
    catchPhrase: string;

    constructor(name: string, bs: string, catchPhrase: string) {
        this.name = name;
        this.bs = bs;
        this.catchPhrase = catchPhrase;
    }
}
